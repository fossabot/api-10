scalar Date

schema {
    query: Query
}

# 查询接口
type Query {
    # 系统时间
    systemTime: String!
    sslCert(domain: String!): SSLCertificate
}


# SSL 证书信息
type SSLCertificate {
    domain: String!
    expireAt: Date
}