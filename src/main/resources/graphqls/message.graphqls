extend type Mutation {
    # 发送消息
    sendMessage(totpCode: Int!, input: SendMessageInput!): MessagePayload

    # 保存WEB订阅信息
    savePushSubscription(subscription: PushSubscriptionInput!): MessagePayload
}