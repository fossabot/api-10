extend type Query {
    # 剧集搜索
    resources(keyword: String!): [ResourceInfo]

    # 剧集详情
    resourceDetail(resourceId: String!): ResourceDetail

    # 剧集分集链接
    episodes(resourceId: String!): [ResourceEpisode]
}